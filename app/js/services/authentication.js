"use strict";

app.factory('authentication', function ($http, BASE_URL) {
    var service = {};
    
    var serviceURL = BASE_URL + 'Account';

    service.Register = function () {
        
    }
});
